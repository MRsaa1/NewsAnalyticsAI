# 🚀 Деплой Signal Analysis на DigitalOcean

> **Техническое задание для разработчика**

---

## 📋 Описание проекта

**Signal Analysis** - профессиональная система анализа финансовых новостей с AI в стиле Bloomberg Terminal.

**Текущий статус**: Полностью работает локально на macOS (http://localhost:8080)

**Задача**: Развернуть ТОЧНО ТАК ЖЕ на сервере DigitalOcean и подключить к домену

---

## 🎯 Требования к деплою

### Обязательно:

```
✅ Развернуть на DigitalOcean (VPS/Droplet)
✅ Настроить домен (предоставит клиент)
✅ SSL сертификат (Let's Encrypt)
✅ Автозапуск при перезагрузке сервера
✅ Логирование
✅ Мониторинг работоспособности
✅ Backup базы данных
```

### Запрещено:

```
❌ НЕ менять функционал
❌ НЕ менять дизайн
❌ НЕ менять структуру базы данных
❌ НЕ добавлять новые зависимости
```

**Деплой "как есть" - без изменений!**

---

## 💻 Технический стек проекта

### Backend
```
Python: 3.10+
Framework: FastAPI 0.104.1
Server: Uvicorn 0.24.0
Database: SQLite 3
Scheduler: APScheduler 3.10.4
```

### Frontend
```
HTML5 + CSS3 (встроено в app.py)
Vanilla JavaScript (без фреймворков)
```

### Dependencies
```python
fastapi==0.104.1
uvicorn[standard]==0.24.0
httpx==0.25.0
feedparser==6.0.10
beautifulsoup4==4.12.2
lxml==4.9.3
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0
apscheduler==3.10.4
reportlab==4.0.7
pillow==10.1.0
aiosqlite==0.19.0
tenacity==8.2.3
```

---

## 📁 Структура проекта

```
signal-analysis/
├── app.py                      # Главный файл (2,500+ строк)
├── requirements.txt            # Python зависимости
├── .env                        # API ключи (СЕКРЕТНЫЙ!)
├── signals.db                  # SQLite база данных
├── signals.db-wal             # WAL журнал
├── signals.db-shm             # Shared memory
├── app.log                     # Логи приложения
├── go-signal-analysis/        # Go Dashboard (опционально)
│   └── signal-analysis-server
└── manage_services.sh         # Скрипт управления (для macOS)
```

---

## 🔑 Переменные окружения (.env)

**ОБЯЗАТЕЛЬНЫЕ**:

```env
# OpenAI API (для основного анализа новостей)
OPENAI_API_KEY=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

# DeepSeek API (для кнопки "Анализ")
DEEPSEEK_API_KEY=sk-d4fcd7e5038f41389078a7502bcb0659
```

**ОПЦИОНАЛЬНЫЕ** (можно не добавлять):

```env
# Telegram (пока отключен)
TELEGRAM_TOKEN=
TELEGRAM_CHANNEL_RU=

# xAI (не используется)
XAI_API_KEY=

# Другие
DRY_RUN=false
MOCK_LLM=false
```

---

## 🌐 Текущее состояние проекта

### Что работает:

```
✅ Dashboard на http://localhost:8080/dashboard
✅ Автоматический сбор новостей каждый час
✅ AI-анализ через GPT-4o (OpenAI) и DeepSeek
✅ База данных: 926 сигналов (последние 7 дней)
✅ Автоочистка данных старше 7 дней
✅ Фильтрация по секторам, влиянию, достоверности
✅ Мультиязычность (RU/EN)
✅ Экспорт в CSV и PDF
✅ Кнопка "Анализ" для детального AI-анализа
```

### Что отключено:

```
❌ Telegram Digest (кнопка удалена)
❌ Go Dashboard (не используется, порт 8090)
```

### Активные секторы (9):

```
1. TREASURY - регуляторы (SEC, ФРС, центробанки)
2. CRYPTO - криптовалюты
3. BIOTECH - биотехнологии и FDA
4. SEMIS - полупроводники
5. ENERGY - энергетика и нефть
6. FINTECH - финансовые технологии
7. COMMODITIES - сырьевые товары
8. EMERGING_MARKETS - развивающиеся рынки
9. TECHNOLOGY - технологии
```

---

## 🖥️ Требования к серверу DigitalOcean

### Минимальная конфигурация:

```
💻 CPU: 2 vCPU
💾 RAM: 4 GB
💿 Диск: 80 GB SSD
🌍 Регион: любой (рекомендуется близко к пользователям)
💰 Стоимость: ~$24/месяц (Droplet Basic)
```

### Операционная система:

```
Ubuntu 22.04 LTS x64 (рекомендуется)
```

---

## 🔧 Инструкция по деплою (для разработчика)

### Шаг 1: Создание Droplet на DigitalOcean

1. Войти в DigitalOcean
2. Create → Droplets
3. Выбрать:
   - **Image**: Ubuntu 22.04 LTS
   - **Plan**: Basic, 4GB RAM, 2 vCPU, 80GB SSD ($24/mo)
   - **Region**: выбрать ближайший к пользователям
   - **Authentication**: SSH ключ (рекомендуется)
4. Создать Droplet

### Шаг 2: Подключение к серверу

```bash
# SSH подключение
ssh root@YOUR_DROPLET_IP

# Обновление системы
apt update && apt upgrade -y

# Установка необходимых пакетов
apt install -y python3.10 python3-pip python3-venv git nginx certbot python3-certbot-nginx supervisor
```

### Шаг 3: Создание пользователя

```bash
# Создать пользователя для приложения
adduser signalapp
usermod -aG sudo signalapp

# Переключиться на нового пользователя
su - signalapp
```

### Шаг 4: Загрузка проекта

```bash
# Создать директорию
mkdir -p /home/signalapp/signal-analysis
cd /home/signalapp/signal-analysis

# Загрузить файлы проекта (один из вариантов):

# Вариант 1: Через Git (если есть репозиторий)
git clone https://github.com/YOUR_REPO/signal-analysis.git .

# Вариант 2: Через SCP (с локальной машины)
# На локальной машине:
# cd ~/signal-analysis
# tar -czf signal-analysis.tar.gz app.py requirements.txt .env signals.db
# scp signal-analysis.tar.gz signalapp@YOUR_DROPLET_IP:/home/signalapp/
# 
# На сервере:
# tar -xzf signal-analysis.tar.gz
```

### Шаг 5: Установка Python зависимостей

```bash
cd /home/signalapp/signal-analysis

# Создать виртуальное окружение
python3 -m venv venv

# Активировать
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### Шаг 6: Настройка .env

```bash
# Создать .env файл
nano .env
```

**Скопировать в .env**:

```env
OPENAI_API_KEY=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
DEEPSEEK_API_KEY=sk-d4fcd7e5038f41389078a7502bcb0659
```

### Шаг 7: Тестовый запуск

```bash
# Активировать venv
source venv/bin/activate

# Запустить сервер
uvicorn app:app --host 0.0.0.0 --port 8080

# Проверить в браузере
# http://YOUR_DROPLET_IP:8080/dashboard
```

**Если работает - переходим к следующему шагу!**

### Шаг 8: Настройка Supervisor (автозапуск)

```bash
# Выйти из виртуального окружения
deactivate

# Создать конфиг Supervisor (от root)
sudo nano /etc/supervisor/conf.d/signal-analysis.conf
```

**Содержимое файла**:

```ini
[program:signal-analysis]
directory=/home/signalapp/signal-analysis
command=/home/signalapp/signal-analysis/venv/bin/uvicorn app:app --host 127.0.0.1 --port 8080 --workers 1
user=signalapp
autostart=true
autorestart=true
stopasgroup=true
killasgroup=true
stderr_logfile=/var/log/signal-analysis/error.log
stdout_logfile=/var/log/signal-analysis/access.log
environment=PATH="/home/signalapp/signal-analysis/venv/bin"
```

**Создать директорию для логов**:

```bash
sudo mkdir -p /var/log/signal-analysis
sudo chown signalapp:signalapp /var/log/signal-analysis
```

**Запустить Supervisor**:

```bash
# Перечитать конфиги
sudo supervisorctl reread

# Обновить
sudo supervisorctl update

# Запустить приложение
sudo supervisorctl start signal-analysis

# Проверить статус
sudo supervisorctl status signal-analysis
```

**Должно быть**: `signal-analysis RUNNING pid XXXXX, uptime X:XX:XX`

### Шаг 9: Настройка Nginx (Reverse Proxy)

```bash
# Создать конфиг Nginx
sudo nano /etc/nginx/sites-available/signal-analysis
```

**Содержимое файла**:

```nginx
server {
    listen 80;
    server_name YOUR_DOMAIN.com www.YOUR_DOMAIN.com;
    
    client_max_body_size 20M;
    
    # Логи
    access_log /var/log/nginx/signal-analysis-access.log;
    error_log /var/log/nginx/signal-analysis-error.log;
    
    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Таймауты для длинных запросов (AI анализ)
        proxy_connect_timeout 300;
        proxy_send_timeout 300;
        proxy_read_timeout 300;
        send_timeout 300;
    }
}
```

**Активировать конфиг**:

```bash
# Создать симлинк
sudo ln -s /etc/nginx/sites-available/signal-analysis /etc/nginx/sites-enabled/

# Проверить конфигурацию
sudo nginx -t

# Перезапустить Nginx
sudo systemctl restart nginx
```

### Шаг 10: Настройка SSL (HTTPS)

```bash
# Получить SSL сертификат от Let's Encrypt
sudo certbot --nginx -d YOUR_DOMAIN.com -d www.YOUR_DOMAIN.com

# Следовать инструкциям Certbot
# Email: YOUR_EMAIL
# Agree to terms: Yes
# Redirect HTTP to HTTPS: Yes (рекомендуется)
```

**Certbot автоматически**:
- Получит сертификат
- Настроит Nginx
- Настроит автообновление

### Шаг 11: Настройка Firewall

```bash
# Настроить UFW firewall
sudo ufw allow OpenSSH
sudo ufw allow 'Nginx Full'
sudo ufw enable

# Проверить статус
sudo ufw status
```

### Шаг 12: Финальная проверка

```bash
# Проверить что приложение запущено
sudo supervisorctl status signal-analysis

# Проверить логи
tail -f /var/log/signal-analysis/access.log

# Проверить Nginx
sudo systemctl status nginx

# Проверить SSL
curl -I https://YOUR_DOMAIN.com
```

**Открыть в браузере**:
```
https://YOUR_DOMAIN.com/dashboard?lang=en
```

---

## 📊 Текущие файлы проекта

### Основные файлы (ОБЯЗАТЕЛЬНО перенести):

```
✅ app.py (2,500+ строк) - главный файл
✅ requirements.txt - зависимости Python
✅ .env - API ключи (СЕКРЕТНО!)
✅ signals.db - база данных (926 записей)
```

### Опциональные файлы:

```
⚠️ signals.db-wal, signals.db-shm - можно не переносить (создадутся автоматически)
⚠️ app.log - можно не переносить
⚠️ go-signal-analysis/ - НЕ используется, можно не переносить
⚠️ Все .md файлы - документация, можно не переносить
```

---

## ⚙️ Настройки приложения

### Порты:

```
Backend: 8080 (внутренний)
Nginx: 80 → 443 (внешний)
```

### Автоматизация:

```
✅ Сбор новостей: каждые 60 минут
✅ Автоочистка: при каждом сборе (удаляет старше 7 дней)
✅ Только новости за сегодня
```

### База данных:

```
Тип: SQLite 3
Файл: signals.db
Размер: ~10-15 MB
Записей: ~900-1000 (7 дней)
WAL режим: включен
```

---

## 🔍 Функционал системы

### 1. Автоматический сбор новостей

**Источники**: 20+ RSS фидов (CryptoNews, SEC, FDA, Bloomberg и т.д.)

**Процесс**:
```
Каждый час:
  1. Загрузить RSS фиды
  2. Фильтр: только новости за сегодня
  3. Дедупликация по URL hash
  4. Анализ через GPT-4o (OpenAI)
  5. Сохранение в базу
  6. Удаление данных старше 7 дней
```

**Логи**: пишутся в `app.log`

### 2. Dashboard (веб-интерфейс)

**URL**: `/dashboard?lang=en` или `/dashboard?lang=ru`

**Возможности**:
- Фильтрация по секторам (CRYPTO, TECH и т.д.)
- Фильтрация по регионам (GLOBAL, USA, ASIA и т.д.)
- Фильтрация по метрикам (Impact, Confidence, Sentiment)
- Поиск по ключевым словам
- Переключение языка (RU/EN)
- Кнопка "📊 Анализ" - детальный AI-анализ через DeepSeek
- Кнопка "📊 EXPORT DATA" - экспорт в CSV/PDF

**Дизайн**: Bloomberg Terminal Style (темная тема, золотые акценты)

### 3. API Endpoints

```
GET  /dashboard              - Веб-интерфейс
GET  /signals                - JSON список сигналов
GET  /health                 - Проверка статуса
POST /ingest-run             - Ручной запуск сбора
POST /generate-analysis/:id  - Генерация анализа
GET  /export/signals.csv     - Экспорт CSV
GET  /export/signals.pdf     - Экспорт PDF
GET  /docs                   - Swagger документация
```

---

## 🔐 Безопасность

### Текущая реализация:

```
✅ API ключи в .env (не в коде)
✅ SQL injection protection (Pydantic)
✅ Rate limiting через semaphore
✅ Localhost only (локально)
```

### Требования для production:

```
✅ HTTPS обязательно (Let's Encrypt)
✅ Firewall настроить (UFW)
✅ .env файл с правами 600
✅ Регулярные backup базы данных
✅ Мониторинг логов
```

---

## 📦 Backup и восстановление

### Что нужно бэкапить:

```
1. signals.db - база данных (КРИТИЧНО!)
2. .env - конфигурация (СЕКРЕТНО!)
3. app.py - код приложения
```

### Скрипт автоматического backup:

```bash
#!/bin/bash
# /home/signalapp/backup.sh

BACKUP_DIR="/home/signalapp/backups"
DATE=$(date +%Y%m%d_%H%M%S)

mkdir -p $BACKUP_DIR

# Backup базы данных
cp /home/signalapp/signal-analysis/signals.db $BACKUP_DIR/signals_$DATE.db

# Удалить backup старше 30 дней
find $BACKUP_DIR -name "signals_*.db" -mtime +30 -delete

echo "Backup completed: signals_$DATE.db"
```

**Настроить cron (ежедневный backup в 3:00)**:

```bash
crontab -e

# Добавить строку:
0 3 * * * /home/signalapp/backup.sh >> /home/signalapp/backup.log 2>&1
```

---

## 📊 Мониторинг

### Health Check

```bash
# Проверка статуса
curl http://localhost:8080/health

# Должен вернуть:
{
  "status": "ok",
  "signals_count": 926,
  "last_update": "2025-11-07T12:30:00Z"
}
```

### Логи

```bash
# Логи приложения
tail -f /var/log/signal-analysis/access.log
tail -f /var/log/signal-analysis/error.log

# Логи Nginx
tail -f /var/log/nginx/signal-analysis-access.log
tail -f /var/log/nginx/signal-analysis-error.log

# Логи Supervisor
sudo supervisorctl tail -f signal-analysis
```

### Uptime мониторинг

Рекомендуется настроить:
- **UptimeRobot** (бесплатный)
- **StatusCake** (бесплатный)
- Проверка каждые 5 минут: `https://YOUR_DOMAIN.com/health`

---

## 🚨 Устранение проблем

### Приложение не запускается

```bash
# Проверить логи
sudo supervisorctl tail signal-analysis stderr

# Проверить зависимости
source /home/signalapp/signal-analysis/venv/bin/activate
pip list

# Проверить .env
cat /home/signalapp/signal-analysis/.env
```

### База данных заблокирована

```bash
# Остановить приложение
sudo supervisorctl stop signal-analysis

# Проверить WAL файлы
ls -lah /home/signalapp/signal-analysis/signals.db*

# Удалить WAL (безопасно если приложение остановлено)
rm /home/signalapp/signal-analysis/signals.db-wal
rm /home/signalapp/signal-analysis/signals.db-shm

# Запустить заново
sudo supervisorctl start signal-analysis
```

### Nginx ошибки

```bash
# Проверить конфигурацию
sudo nginx -t

# Проверить логи
tail -50 /var/log/nginx/error.log

# Перезапустить
sudo systemctl restart nginx
```

### SSL сертификат не обновляется

```bash
# Проверить статус
sudo certbot renew --dry-run

# Принудительное обновление
sudo certbot renew --force-renewal
```

---

## 📋 Чек-лист для разработчика

### Перед деплоем:

```
☐ Создать Droplet на DigitalOcean (4GB RAM)
☐ Настроить DNS A-запись (домен → IP сервера)
☐ Подготовить SSH ключи
☐ Сохранить API ключи из .env
```

### Во время деплоя:

```
☐ Подключиться к серверу по SSH
☐ Обновить систему (apt update && upgrade)
☐ Установить Python 3.10+
☐ Установить Nginx, Certbot, Supervisor
☐ Создать пользователя signalapp
☐ Загрузить файлы проекта
☐ Установить Python зависимости (venv + pip)
☐ Создать .env с API ключами
☐ Протестировать запуск вручную
☐ Настроить Supervisor для автозапуска
☐ Настроить Nginx reverse proxy
☐ Получить SSL сертификат (Certbot)
☐ Настроить Firewall (UFW)
☐ Настроить backup (cron)
```

### После деплоя:

```
☐ Проверить доступность: https://YOUR_DOMAIN.com/dashboard
☐ Проверить автозапуск (перезагрузить сервер)
☐ Проверить SSL (https работает)
☐ Проверить логи (нет ошибок)
☐ Проверить функционал:
   ☐ Загрузка сигналов работает
   ☐ Фильтры работают
   ☐ Кнопка "Анализ" работает
   ☐ Экспорт CSV/PDF работает
   ☐ Переключение языка работает
☐ Настроить мониторинг (UptimeRobot)
☐ Проверить backup (должен создаться через сутки)
☐ Передать клиенту доступы
```

---

## 📞 Контакты для разработчика

### Что нужно от клиента:

```
1. Доменное имя (например: analytics.example.com)
2. Доступ к DigitalOcean (или создать Droplet самостоятельно)
3. Email для SSL сертификата
4. Подтверждение что API ключи работают
```

### Что передать клиенту после деплоя:

```
✅ URL сайта: https://YOUR_DOMAIN.com/dashboard
✅ SSH доступ к серверу
✅ Логин/пароль от DigitalOcean (если создавали)
✅ Инструкция по мониторингу
✅ Расписание backup
✅ Контакты для поддержки
```

---

## 💰 Стоимость содержания

### DigitalOcean:

```
Droplet (4GB): $24/месяц
Backup (опционально): $4.80/месяц
Total: ~$30/месяц
```

### API вызовы:

```
OpenAI GPT-4o: ~$10-30/месяц (зависит от количества новостей)
DeepSeek: ~$1-3/месяц (очень дешево)
Total: ~$11-33/месяц
```

### Итого:

```
Сервер: $30/месяц
API: $11-33/месяц
Total: ~$41-63/месяц
```

---

## 🎯 Важные примечания

### 1. База данных SQLite

```
✅ Подходит для <1M записей
✅ Простой деплой (один файл)
✅ Автоматический WAL режим
⚠️ Один пользователь одновременно
```

**Если нужна масштабируемость** (много пользователей):
- Мигрировать на PostgreSQL
- Но для текущей задачи SQLite достаточно!

### 2. Горизонтальное масштабирование

Текущая реализация:
```
❌ НЕ поддерживает несколько серверов (SQLite ограничение)
✅ Достаточно для 100-1000 пользователей/день
```

### 3. Производительность

```
✅ Dashboard загрузка: <100ms
✅ API запросы: <50ms
✅ Сбор новостей: ~5 минут (каждый час)
✅ AI анализ через DeepSeek: ~5 секунд
✅ AI анализ через GPT-4o: ~10 секунд
```

---

## 📝 Команды управления на сервере

### Управление приложением:

```bash
# Статус
sudo supervisorctl status signal-analysis

# Остановить
sudo supervisorctl stop signal-analysis

# Запустить
sudo supervisorctl start signal-analysis

# Перезапустить
sudo supervisorctl restart signal-analysis

# Логи (live)
sudo supervisorctl tail -f signal-analysis
```

### Обновление кода:

```bash
# Зайти на сервер
ssh signalapp@YOUR_DROPLET_IP

# Перейти в директорию
cd /home/signalapp/signal-analysis

# Остановить приложение
sudo supervisorctl stop signal-analysis

# Обновить код (если через Git)
git pull

# ИЛИ загрузить новый app.py через SCP
# (на локальной машине):
# scp app.py signalapp@YOUR_DROPLET_IP:/home/signalapp/signal-analysis/

# Запустить
sudo supervisorctl start signal-analysis
```

---

## 🧪 Тестирование после деплоя

### 1. Проверка доступности

```bash
# Проверить HTTP → HTTPS редирект
curl -I http://YOUR_DOMAIN.com

# Проверить HTTPS
curl -I https://YOUR_DOMAIN.com

# Проверить API
curl https://YOUR_DOMAIN.com/health
```

### 2. Проверка функционала

```
✅ Открыть: https://YOUR_DOMAIN.com/dashboard?lang=en
✅ Выбрать фильтры (CRYPTO, Impact 80+)
✅ Нажать: "🔍 LOAD SIGNALS"
✅ Проверить: новости загружаются
✅ Нажать: кнопку "Анализ" на новости
✅ Проверить: аналитика генерируется (DeepSeek)
✅ Переключить язык: RU ↔ EN
✅ Экспорт: "📊 EXPORT DATA" → CSV
```

### 3. Проверка автоматизации

```bash
# Проверить что сбор новостей работает
# Подождать 1 час или запустить вручную:
curl -X POST https://YOUR_DOMAIN.com/ingest-run

# Проверить логи
tail -f /var/log/signal-analysis/access.log | grep PIPELINE
```

---

## 📚 Документация

На сервер можно НЕ переносить, но для справки:

```
📚_ДОКУМЕНТАЦИЯ.md - главный индекс (19 KB)
ПОЛНАЯ_ДОКУМЕНТАЦИЯ.md - все детали (40 KB)
АРХИТЕКТУРА.md - схемы (32 KB)
НАВИГАЦИЯ.md - карта проекта (12 KB)
ССЫЛКИ.md - команды (11 KB)
```

Всего: ~150 страниц документации

---

## 🎯 Итоговое техническое задание

### Задача:

Развернуть проект **Signal Analysis** "как есть" на сервере DigitalOcean с доменом.

### Что должно работать после деплоя:

```
✅ https://YOUR_DOMAIN.com/dashboard - веб-интерфейс
✅ Автоматический сбор новостей каждый час
✅ AI-анализ через GPT-4o и DeepSeek
✅ База данных хранит последние 7 дней
✅ Фильтрация, поиск, экспорт работают
✅ Мультиязычность (RU/EN)
✅ SSL сертификат
✅ Автозапуск при перезагрузке сервера
✅ Логирование
✅ Backup базы данных
```

### Срок выполнения:

```
Ориентировочно: 4-6 часов работы
```

### Что передать разработчику:

```
1. Этот файл (DEPLOYMENT_DIGITALOCEAN.md)
2. Файл app.py
3. Файл requirements.txt
4. Файл .env (с API ключами)
5. База данных signals.db
```

---

## 📞 Поддержка

После деплоя разработчик должен предоставить:

```
✅ URL сайта
✅ SSH доступ для администрирования
✅ Документацию по управлению
✅ Контакты для экстренной поддержки
✅ Инструкцию по обновлению кода
```

---

**Подготовлено**: 7 ноября 2025  
**Версия проекта**: 1.6.1  
**Размер проекта**: app.py (2,500 строк) + зависимости

