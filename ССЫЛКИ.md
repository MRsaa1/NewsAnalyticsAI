# 🔗 Быстрые ссылки - Signal Analysis

> **Все важные ссылки в одном месте**

---

## 🌐 Dashboard (Web UI)

### Русская версия
```
http://localhost:8080/dashboard?lang=ru
```

### English version
```
http://localhost:8080/dashboard?lang=en
```

### Go Dashboard (альтернативный)
```
http://localhost:8090/dashboard
```

---

## 📡 API Endpoints

### Основные

**Список сигналов**
```
GET http://localhost:8080/signals
```

**С фильтрами**
```
GET http://localhost:8080/signals?sector=CRYPTO&min_impact=80
```

**Статус системы**
```
GET http://localhost:8080/health
```

### Управление

**Запустить сбор новостей**
```
POST http://localhost:8080/ingest-run
```

**Telegram дайджест**
```
GET http://localhost:8080/telegram-digest?send=true&top_n=10
```

### Экспорт

**Экспорт в CSV**
```
GET http://localhost:8080/export/signals.csv
```

**Экспорт в PDF**
```
GET http://localhost:8080/export/signals.pdf
```

**Экспорт CRYPTO сектора**
```
GET http://localhost:8080/export/signals.csv?sector=CRYPTO
```

### Swagger документация

```
http://localhost:8080/docs
```

---

## 📚 Документация

### Главные документы

| Документ | Описание | Ссылка |
|----------|----------|--------|
| 📚 Индекс | Навигация по всей документации | [📚_ДОКУМЕНТАЦИЯ.md](📚_ДОКУМЕНТАЦИЯ.md) |
| 📖 Полная документация | 15,000+ слов, все детали | [ПОЛНАЯ_ДОКУМЕНТАЦИЯ.md](ПОЛНАЯ_ДОКУМЕНТАЦИЯ.md) |
| 📍 Навигация | Карта проекта | [НАВИГАЦИЯ.md](НАВИГАЦИЯ.md) |
| 🏗️ Архитектура | Технические схемы | [АРХИТЕКТУРА.md](АРХИТЕКТУРА.md) |
| 🚀 Быстрый старт | Начните здесь | [START_HERE.md](START_HERE.md) |
| ⚙️ Управление | Сервисы и команды | [SERVICES.md](SERVICES.md) |
| 🔧 Конфигурация | Настройки системы | [config.md](config.md) |

### Быстрый доступ

**Начните с этих файлов:**

1. **[📚_ДОКУМЕНТАЦИЯ.md](📚_ДОКУМЕНТАЦИЯ.md)** - начальная точка
2. **[ПОЛНАЯ_ДОКУМЕНТАЦИЯ.md](ПОЛНАЯ_ДОКУМЕНТАЦИЯ.md)** - всё о проекте
3. **[НАВИГАЦИЯ.md](НАВИГАЦИЯ.md)** - как найти нужное

---

## 💻 Команды терминала

### Управление сервисами

**Запустить**
```bash
./manage_services.sh start
```

**Остановить**
```bash
./manage_services.sh stop
```

**Перезапустить**
```bash
./manage_services.sh restart
```

**Статус**
```bash
./manage_services.sh status
```

**Логи Python**
```bash
./manage_services.sh logs python
```

**Логи Go**
```bash
./manage_services.sh logs go
```

### Диагностика

**Полная диагностика**
```bash
python debug.py
```

**Проверить логи**
```bash
tail -f app.log
tail -50 app.log
grep ERROR app.log
```

**Проверить порты**
```bash
lsof -i :8080
lsof -i :8090
```

**Проверить процессы**
```bash
ps aux | grep python
ps aux | grep signal-analysis
```

### Работа с базой данных

**Открыть SQLite**
```bash
sqlite3 signals.db
```

**Схема базы**
```bash
sqlite3 signals.db ".schema signals"
```

**Статистика**
```bash
sqlite3 signals.db "SELECT COUNT(*) FROM signals"
sqlite3 signals.db "SELECT sector, COUNT(*) FROM signals GROUP BY sector"
```

**Последние новости**
```bash
sqlite3 signals.db "SELECT title, sector, impact FROM signals ORDER BY ts_published DESC LIMIT 10"
```

### Очистка старых данных

**⚠️ ВАЖНО**: Старые сигналы НЕ удаляются автоматически!

**Статистика по датам**
```bash
python3 cleanup_old_signals.py --stats
```

**Тестовый режим (безопасно)**
```bash
python3 cleanup_old_signals.py --days 30      # Показать что удалится
python3 cleanup_old_signals.py --days 90      # За 90 дней
```

**Реальное удаление**
```bash
python3 cleanup_old_signals.py --days 30 --execute   # Удалить старше 30 дней
python3 cleanup_old_signals.py --days 90 --execute   # Удалить старше 90 дней
```

**Ручное удаление**
```bash
# Удалить старше 30 дней
sqlite3 signals.db "DELETE FROM signals WHERE DATE(ts_published) < DATE('now', '-30 days')"

# Оптимизировать базу
sqlite3 signals.db "VACUUM"
```

### API запросы (curl)

**Получить сигналы**
```bash
curl http://localhost:8080/signals
curl http://localhost:8080/signals | jq
```

**Статус системы**
```bash
curl http://localhost:8080/health | jq
```

**Запустить сбор**
```bash
curl -X POST http://localhost:8080/ingest-run | jq
```

**Скачать CSV**
```bash
curl -o signals.csv http://localhost:8080/export/signals.csv
```

**Криптовалюты с impact > 80**
```bash
curl "http://localhost:8080/signals?sector=CRYPTO&min_impact=80" | jq
```

---

## 📁 Важные файлы

### Код

| Файл | Строки | Описание |
|------|--------|----------|
| `app.py` | 2441 | Главный Python backend |
| `go-signal-analysis/main.go` | ~800 | Go Dashboard |
| `requirements.txt` | 15 | Python зависимости |
| `manage_services.sh` | ~200 | Скрипт управления |

### База данных

```
signals.db          - Главная база
signals.db-wal      - Write-Ahead Log
signals.db-shm      - Shared Memory
```

### Логи

```
app.log             - Python сервер
server.log          - Go сервер
```

### Конфигурация

```
.env                - API ключи (секретные)
env.example         - Пример конфига
```

---

## 🔑 Переменные окружения (.env)

### Обязательные

```env
# LLM (нужен хотя бы один)
OPENAI_API_KEY=sk-your-openai-key-here
DEEPSEEK_API_KEY=sk-your-deepseek-key-here
```

### Опциональные

```env
# Дополнительный LLM
XAI_API_KEY=your-xai-key-here

# Telegram
TELEGRAM_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHANNEL_RU=@your_channel

# Режимы разработки
DRY_RUN=false
MOCK_LLM=false
```

---

## 🎯 Частые задачи

### Запустить систему

```bash
cd ~/signal-analysis
./manage_services.sh start
open http://localhost:8080/dashboard?lang=ru
```

### Проверить статус

```bash
./manage_services.sh status
curl http://localhost:8080/health
```

### Собрать новости

```bash
curl -X POST http://localhost:8080/ingest-run
```

### Посмотреть логи

```bash
tail -f app.log
```

### Экспортировать данные

```bash
# CSV
curl -o signals.csv http://localhost:8080/export/signals.csv

# PDF
curl -o signals.pdf http://localhost:8080/export/signals.pdf
```

### Отправить в Telegram

```bash
curl "http://localhost:8080/telegram-digest?send=true"
```

### Перезапустить при проблемах

```bash
./manage_services.sh restart
python debug.py
```

---

## 🌍 Локальные пути

### Проект

```
~/signal-analysis/
```

### База данных

```
~/signal-analysis/signals.db
```

### Логи

```
~/signal-analysis/app.log
~/signal-analysis/server.log
```

### Конфигурация

```
~/signal-analysis/.env
```

### Автозапуск (macOS)

```
~/Library/LaunchAgents/com.signalanalysis.python.plist
~/Library/LaunchAgents/com.signalanalysis.go.plist
```

---

## 🛠️ Полезные команды

### Одной строкой

**Запустить всё**
```bash
cd ~/signal-analysis && ./manage_services.sh start && sleep 3 && open http://localhost:8080/dashboard?lang=ru
```

**Статус + последние логи**
```bash
./manage_services.sh status && echo "=== LOGS ===" && tail -20 app.log
```

**Перезапуск + мониторинг**
```bash
./manage_services.sh restart && tail -f app.log
```

**Экспорт всех крипто-сигналов**
```bash
curl "http://localhost:8080/export/signals.csv?sector=CRYPTO" -o crypto_$(date +%Y%m%d).csv
```

**Статистика по базе**
```bash
sqlite3 signals.db "SELECT sector, COUNT(*) as count FROM signals GROUP BY sector ORDER BY count DESC"
```

---

## 📞 Помощь

### Если не работает

```bash
# 1. Диагностика
python debug.py

# 2. Логи
tail -50 app.log | grep ERROR

# 3. Проверить процессы
./manage_services.sh status

# 4. Перезапустить
./manage_services.sh restart

# 5. Проверить порты
lsof -i :8080 -i :8090
```

### Где искать решение

1. **ПОЛНАЯ_ДОКУМЕНТАЦИЯ.md** → "Устранение неполадок"
2. **Логи**: app.log, server.log
3. **Статус**: `./manage_services.sh status`
4. **Диагностика**: `python debug.py`

---

## 📊 Мониторинг

### Real-time логи

**Python сервер**
```bash
tail -f app.log
```

**Go сервер**
```bash
tail -f server.log
```

**Оба сразу**
```bash
tail -f app.log server.log
```

### Статистика

**Системная**
```bash
curl http://localhost:8080/health | jq
```

**База данных**
```bash
sqlite3 signals.db "SELECT 
    COUNT(*) as total,
    MAX(ts_published) as last_news,
    AVG(impact) as avg_impact
FROM signals"
```

**Использование ресурсов**
```bash
ps aux | grep -E "(python|signal-analysis)" | awk '{print $2, $3, $4, $11}'
```

---

## 🎓 Обучение

### Новичок (начните здесь)

1. [START_HERE.md](START_HERE.md) - быстрый старт
2. [НАВИГАЦИЯ.md](НАВИГАЦИЯ.md) - карта проекта
3. Dashboard - http://localhost:8080/dashboard?lang=ru

### Пользователь

1. [ПОЛНАЯ_ДОКУМЕНТАЦИЯ.md](ПОЛНАЯ_ДОКУМЕНТАЦИЯ.md) - функционал
2. [config.md](config.md) - настройки
3. API - http://localhost:8080/docs

### Разработчик

1. [ПОЛНАЯ_ДОКУМЕНТАЦИЯ.md](ПОЛНАЯ_ДОКУМЕНТАЦИЯ.md) - всё
2. [АРХИТЕКТУРА.md](АРХИТЕКТУРА.md) - архитектура
3. `app.py` - код

---

## 📝 Шпаргалка

### Главные ссылки

```
Dashboard:  http://localhost:8080/dashboard?lang=ru
API:        http://localhost:8080/signals
Health:     http://localhost:8080/health
Swagger:    http://localhost:8080/docs
```

### Главные команды

```bash
./manage_services.sh start    # Запустить
./manage_services.sh status   # Статус
python debug.py               # Диагностика
tail -f app.log               # Логи
```

### Главные файлы

```
📚_ДОКУМЕНТАЦИЯ.md           # Индекс документации
ПОЛНАЯ_ДОКУМЕНТАЦИЯ.md       # Главная документация
app.py                       # Главный код
signals.db                   # База данных
.env                         # Конфигурация
```

---

**💡 Совет**: Сохраните этот файл в закладки для быстрого доступа!

---

**Последнее обновление**: 6 ноября 2025  
**Версия**: 1.5.0

