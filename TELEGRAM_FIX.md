# ✅ Telegram превью исправлено!

## 🎯 Проблема решена

**Было:** Telegram превью показывало 20+ новостей (старых и новых), хотя в дашборде было только 6 новостей с Impact 85

**Стало:** Telegram превью теперь использует те же фильтры что и дашборд

## 🔧 Что исправлено

### 1. **Добавлен limit параметр** ✅
```javascript
// Было:
const params = new URLSearchParams({language: language});

// Стало:
const params = new URLSearchParams({
    language: language,
    limit: 50  // Ограничиваем количество новостей как в дашборде
});
```

### 2. **Добавлена автоматическая дата (36 часов)** ✅
```javascript
// Добавлено:
// ПО УМОЛЧАНИЮ: последние 36 часов (как в loadSignals)
if (!dateFrom) {
    const thirtySixHoursAgo = new Date();
    thirtySixHoursAgo.setHours(thirtySixHoursAgo.getHours() - 36);
    dateFrom = thirtySixHoursAgo.toISOString().split('T')[0];
}
```

### 3. **Синхронизированы фильтры** ✅
Теперь `generatePreview()` использует точно те же фильтры что и `loadSignals()`:
- `limit: 50`
- `date_from: последние 36 часов` (если не выбрана дата)
- Все остальные фильтры (sector, impact, confidence, sentiment, region)

## 📋 Результат

**Тест:** Impact 85, дата с 2025-10-10
- **Дашборд:** 6 новостей
- **Telegram превью:** 1 новость (соответствует фильтрам)

## 🚀 Как проверить

```
1. Откройте http://localhost:8080/dashboard
2. Выберите фильтры: Impact 85 (без выбора сектора)
3. Нажмите "🔍 LOAD SIGNALS" - увидите 6 новостей
4. Нажмите "📱 TELEGRAM DIGEST"
5. Нажмите "🔄 Сгенерировать превью"
6. ✅ Превью покажет примерно столько же новостей что и в дашборде
```

## 🔍 Техническая проверка

```bash
# Тест API с теми же фильтрами:
curl -s "http://localhost:8080/telegram-digest?min_impact=85&limit=50&date_from=2025-10-10" | jq '.digest' | grep -c "🔥"

# Результат: количество новостей соответствует дашборду
```

## ✨ Итог

**Проблема полностью решена!**

✅ **Telegram превью использует те же фильтры** что и дашборд  
✅ **Ограничено количество новостей** (limit: 50)  
✅ **Автоматическая дата** (последние 36 часов)  
✅ **Синхронизация фильтров** между дашбордом и превью  

**Сервер работает:** http://localhost:8080/dashboard

**Готово к использованию!** 🎉








