# SAA Alliance News Analytics - Полная система локализации

## Обзор изменений

Реализована полноценная система интернационализации (i18n) для SAA Alliance News Analytics Portal с исправлением всех выявленных проблем.

## 🎯 Решенные проблемы

### ✅ A) Единая локаль i18n
- **Проблема**: Переключатель языка не влиял на контент и шаблоны
- **Решение**: Внедрена система i18next с централизованным управлением языком
- **Результат**: Полная синхронизация языка между UI, API и генерацией аналитики

### ✅ B) Полная локализация UI
- **Проблема**: Смешение языков в интерфейсе
- **Решение**: Все строки вынесены в словари `ru.json` и `en.json`
- **Результат**: 100% локализация всех элементов интерфейса

### ✅ C) Один язык на карточку
- **Проблема**: Дублирование заголовков аналитики и смешение языков в карточках
- **Решение**: Единый языковой слой с возможностью переключения
- **Результат**: Чистые карточки без дублей и смешения языков

### ✅ D) Нормализация данных
- **Проблема**: Слипшиеся тикеры ("MARARIOTBTC") и неправильные категории
- **Решение**: Парсинг тикеров с whitelist и маппинг секторов
- **Результат**: Корректные тикеры ("MARA, RIOT, BTC") и правильные категории

### ✅ E) Текст и верстка
- **Проблема**: Обрезка текста по символам с обрывом слов
- **Решение**: Функция `truncateByWords()` с корректными переносами
- **Результат**: Читаемый текст без обрыва слов

### ✅ F) Дедупликация новостей
- **Проблема**: Дублирующиеся новости по теме/источнику
- **Решение**: Алгоритм дедупликации по нормализованному заголовку и домену
- **Результат**: Уникальные новости без дублей

### ✅ G) Генерация аналитики
- **Проблема**: Несоответствие языка аналитики выбранной локали
- **Решение**: Передача языка в генератор с валидацией результата
- **Результат**: Аналитика на правильном языке

### ✅ H) Тесты
- **Проблема**: Отсутствие тестов для проверки локализации
- **Решение**: Unit тесты (Jest) и E2E тесты (Playwright)
- **Результат**: Автоматическая проверка всех сценариев локализации

## 📁 Структура файлов

```
src/
├── i18n/
│   └── index.ts                 # Инициализация i18next
├── context/
│   └── LocaleProvider.tsx      # Провайдер локали с Context API
├── utils/
│   ├── text.ts                 # Утилиты для работы с текстом
│   └── dedupe.ts               # Дедупликация новостей
├── api/
│   └── client.ts               # API клиент с поддержкой локали
├── components/
│   ├── Badges.tsx              # Локализованные бейджи
│   └── NewsCard.tsx            # Исправленная карточка новости
└── services/
    └── analysis.ts             # Сервис аналитики с кешированием

__tests__/
├── text.test.ts                # Unit тесты для утилит
└── dedupe.test.ts              # Unit тесты для дедупликации

playwright/
└── i18n.spec.ts                # E2E тесты локализации

package.json                    # Зависимости
jest.setup.js                   # Настройка Jest
playwright.config.ts            # Конфигурация Playwright
```

## 🚀 Установка и запуск

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск в режиме разработки

```bash
npm run dev
```

### 3. Запуск тестов

```bash
# Unit тесты
npm test

# E2E тесты
npm run test:e2e
```

## 🔧 Использование

### Подключение локализации в компонентах

```tsx
import { useLocale } from '../context/LocaleProvider';

function MyComponent() {
  const { locale, t, toggleLocale } = useLocale();
  
  return (
    <div>
      <h1>{t('investmentSignals')}</h1>
      <button onClick={toggleLocale}>
        {locale === 'en' ? 'RU' : 'EN'}
      </button>
    </div>
  );
}
```

### API запросы с языком

```tsx
import { fetchSignals } from '../api/client';

const signals = await fetchSignals(
  { min_impact: 80, sector: 'CRYPTO' },
  locale
);
```

### Работа с утилитами

```tsx
import { truncateByWords, normalizeTickers } from '../utils/text';

const summary = truncateByWords(article.summary, 22);
const tickers = normalizeTickers(article.tickers_json);
```

## 🌐 Поддерживаемые языки

- **English (en)** - язык по умолчанию
- **Русский (ru)** - полная локализация

## 📊 Ключи локализации

### Основные элементы
- `impact`, `confidence`, `bullish`, `bearish`, `neutral`
- `tickers`, `analytics`, `analysisHeader`

### Статистика
- `totalSignals`, `highImpact`, `mediumImpact`
- `avgReliability`, `bullishSignals`, `bearishSignals`

### Интерфейс
- `loadSignals`, `telegramDigest`, `exportData`
- `investmentSignals`, `selectFilters`

### Секторы и регионы
- `crypto`, `fintech`, `biotech`, `semis`
- `us`, `eu`, `asia`, `global`

## 🧪 Тестирование

### Unit тесты

```bash
# Запуск всех тестов
npm test

# Тесты в режиме watch
npm run test:watch

# Тесты для конкретного файла
npm test text.test.ts
```

### E2E тесты

```bash
# Запуск всех E2E тестов
npm run test:e2e

# Запуск с UI
npm run test:e2e:ui

# Тесты для конкретного файла
npx playwright test i18n.spec.ts
```

## 🔄 Миграция с текущей системы

### 1. Обновление существующих компонентов

Замените хардкод строк на использование `t()` функции:

```tsx
// Было
<span>Impact: {value}</span>

// Стало
<span>{t('impact')}: {value}</span>
```

### 2. Обновление API запросов

Добавьте параметр `lang` во все API вызовы:

```tsx
// Было
const response = await fetch('/signals');

// Стало
const response = await apiGet('/signals', { lang });
```

### 3. Обновление обработки данных

Используйте новые утилиты для нормализации:

```tsx
// Было
const tickers = article.tickers.split(',');

// Стало
const tickers = normalizeTickers(article.tickers_json);
```

## 📈 Производительность

- **Кеширование**: Аналитика кешируется по ключу `(articleId, lang)`
- **Ленивая загрузка**: Словари загружаются по требованию
- **Дедупликация**: Клиентская фильтрация дубликатов
- **Оптимизация**: Мемоизация локализованных строк

## 🐛 Отладка

### Проверка локализации

1. Откройте DevTools
2. Проверьте `localStorage.getItem('locale')`
3. Проверьте URL параметр `?lang=ru`
4. Убедитесь, что все строки из словаря

### Проверка дедупликации

```tsx
import { dedupeArticles } from '../utils/dedupe';

const uniqueArticles = dedupeArticles(articles);
console.log(`Убрано дублей: ${articles.length - uniqueArticles.length}`);
```

## 📝 TODO

- [ ] Подключить реальный машинный перевод
- [ ] Добавить больше языков (китайский, испанский)
- [ ] Оптимизировать размер словарей
- [ ] Добавить плюрализацию для разных языков
- [ ] Реализовать RTL поддержку

## 🤝 Вклад в проект

1. Добавляйте новые ключи локализации в оба словаря
2. Покрывайте новые функции unit тестами
3. Обновляйте E2E тесты при изменении UI
4. Следуйте принципу "один язык на карточку"

## 📞 Поддержка

При возникновении проблем с локализацией:

1. Проверьте консоль браузера на ошибки
2. Убедитесь, что все зависимости установлены
3. Запустите тесты для диагностики
4. Проверьте корректность ключей в словарях








