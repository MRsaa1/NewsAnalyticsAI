# 📊 Signal Analysis - Полная Документация Проекта

> **Профессиональная система анализа новостей для инвесторов в стиле Bloomberg Terminal**

🌐 **Dashboard**: http://localhost:8080/dashboard  
🌐 **API**: http://localhost:8080/api/signals

---

## 📋 Содержание

1. [Обзор проекта](#обзор-проекта)
2. [Архитектура системы](#архитектура-системы)
3. [Технологический стек](#технологический-стек)
4. [Основной функционал](#основной-функционал)
5. [Структура базы данных](#структура-базы-данных)
6. [API Endpoints](#api-endpoints)
7. [Процесс анализа новостей](#процесс-анализа-новостей)
8. [Интеграции](#интеграции)
9. [Установка и запуск](#установка-и-запуск)
10. [Управление сервисами](#управление-сервисами)
11. [Конфигурация](#конфигурация)
12. [Структура кода](#структура-кода)
13. [Алгоритмы и утилиты](#алгоритмы-и-утилиты)
14. [Локализация](#локализация)
15. [Мониторинг и логирование](#мониторинг-и-логирование)
16. [Устранение неполадок](#устранение-неполадок)

---

## 🎯 Обзор проекта

**Signal Analysis** - это профессиональная аналитическая платформа для анализа финансовых новостей и создания инвестиционных сигналов. Система автоматически собирает новости из RSS-фидов, анализирует их с помощью LLM (GPT-4, DeepSeek), определяет влияние на рынки и секторы, и предоставляет визуальный интерфейс для работы с данными.

### Ключевые особенности

- 🎨 **Bloomberg-style UI** - профессиональный дизайн с темной темой и золотыми акцентами
- 🤖 **AI-анализ** - интеграция с OpenAI GPT-4 и DeepSeek для глубокого анализа
- 📊 **20+ секторов** - от криптовалют до биотехнологий и энергетики
- 🌍 **Мультиязычность** - поддержка русского и английского языков
- 📱 **Telegram** - автоматическая публикация дайджестов
- 📈 **Фильтрация** - по влиянию, достоверности, настроению, секторам
- 📊 **Экспорт** - данные в CSV и PDF форматах
- 🔄 **Автообновление** - сбор новостей каждый час
- 🚀 **Высокая производительность** - async/await, connection pooling

---

## 🏗 Архитектура системы

Проект состоит из двух основных компонентов:

### 1. Python Backend (FastAPI)

**Файл**: `app.py` (2441 строка)

**Отвечает за**:
- Сбор новостей из RSS-фидов
- Анализ через LLM (OpenAI GPT-4, DeepSeek)
- Работа с базой данных SQLite
- REST API для фронтенда
- Автоматический пайплайн обработки
- Генерация Telegram дайджестов
- Экспорт данных в CSV/PDF

**Порт**: `8080`

### 2. Go Backend (Dashboard Server)

**Директория**: `go-signal-analysis/`

**Отвечает за**:
- Быстрый веб-интерфейс (10x быстрее Python)
- Визуализация данных
- Статистика в реальном времени
- API для фронтенда

**Порт**: `8090`

### Схема работы

```
┌──────────────┐
│  RSS Фиды    │
│ (новости)    │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────┐
│   Python Backend (app.py)            │
│                                      │
│  1. Сбор новостей (ingest)          │
│  2. Дедупликация                    │
│  3. Анализ через LLM                │
│  4. Сохранение в БД                 │
│  5. REST API                         │
└──────────┬───────────────────────────┘
           │
           ▼
   ┌───────────────┐
   │   SQLite DB   │
   │  (signals.db) │
   └───────┬───────┘
           │
           ▼
┌──────────────────────────────────────┐
│   Go Backend (Dashboard)             │
│                                      │
│  1. Чтение из БД                    │
│  2. Визуализация                    │
│  3. Статистика                      │
│  4. Фильтрация                      │
└──────────────────────────────────────┘
           │
           ▼
   ┌───────────────┐
   │   Web UI      │
   │  (Dashboard)  │
   └───────────────┘
```

---

## 💻 Технологический стек

### Backend (Python)

```
fastapi==0.104.1          # Веб-фреймворк
uvicorn==0.24.0           # ASGI сервер
httpx==0.25.0             # HTTP клиент
feedparser==6.0.10        # Парсинг RSS
beautifulsoup4==4.12.2    # Парсинг HTML
pydantic==2.5.0           # Валидация данных
apscheduler==3.10.4       # Планировщик задач
reportlab==4.0.7          # Генерация PDF
pillow==10.1.0            # Работа с изображениями
aiosqlite==0.19.0         # Async SQLite
tenacity==8.2.3           # Retry логика
```

### Backend (Go)

```
Go 1.21+
github.com/mattn/go-sqlite3    # SQLite драйвер
net/http (стандартная библиотека)
```

### Frontend

```
HTML5 + CSS3 (встроено в app.py)
Vanilla JavaScript (без фреймворков)
Система i18n для локализации
```

### База данных

```
SQLite 3
- Журналирование: WAL (Write-Ahead Logging)
- Синхронизация: NORMAL
- Таймаут: 60 секунд
```

---

## ⚙️ Основной функционал

### 1. Автоматический сбор новостей

**RSS фиды** (9 активных секторов):
- `TREASURY` - регуляторы (SEC, ФРС, центробанки)
- `CRYPTO` - криптовалюты
- `BIOTECH` - биотехнологии и FDA
- `SEMIS` - полупроводники
- `ENERGY` - энергетика и нефть
- `FINTECH` - финансовые технологии
- `COMMODITIES` - сырьевые товары
- `EMERGING_MARKETS` - развивающиеся рынки
- `TECHNOLOGY` - технологии

**Процесс**:
1. Каждый час запускается планировщик
2. Система обходит все RSS фиды
3. Загружает последние 10 новостей из каждого
4. Проверяет дубликаты по URL hash
5. Сохраняет в таблицу `ingested`

### 2. AI-анализ новостей

**LLM провайдеры**:
- **OpenAI GPT-4** (основной, опционально)
- **DeepSeek** (основной, рекомендуется)
- **xAI Grok** (опционально)

**Промпт анализа**:
```
Вы - профессиональный финансовый аналитик Bloomberg Terminal.
Проанализируйте новость и верните JSON:

{
  "title_en": "заголовок на английском",
  "title_ru": "заголовок на русском",
  "summary_en": "краткое описание на английском",
  "summary_ru": "краткое описание на русском",
  "tickers": ["TSLA", "BTC"],
  "sector": "CRYPTO",
  "region": "GLOBAL",
  "impact": 85,        // 0-100
  "confidence": 90,    // 0-100
  "sentiment": 75,     // 0-100 (0=негатив, 100=позитив)
  "entities": ["Илон Маск", "Tesla"],
  "deep_analysis_en": "глубокий анализ...",
  "deep_analysis_ru": "глубокий анализ..."
}
```

**Метрики**:
- `impact` - сила влияния на рынок (0-100)
- `confidence` - достоверность источника (0-100)
- `sentiment` - настроение рынка (0=негатив, 50=нейтрально, 100=позитив)

### 3. Веб-интерфейс (Dashboard)

**URL**: http://localhost:8080/dashboard?lang=ru

**Возможности**:
- 📊 Карточки новостей с цветовой кодировкой
- 🔍 Фильтрация по секторам, регионам, влиянию
- 🌐 Переключение языка (RU/EN)
- 📈 Статистика в реальном времени
- 🎯 Детальный просмотр анализа
- 📱 Адаптивный дизайн

**Цветовая схема**:
- Фон: `#0a0e1a` (темно-синий)
- Акценты: `#d4af37` (золотой)
- Карточки: `#1a1f35`
- Текст: `#e0e6ed`

### 4. Telegram интеграция

**Endpoint**: `GET /telegram-digest`

**Формат дайджеста**:
```
🌟 Вечерний дайджест
Пятница, 6 ноября 2025

📊 ОБЗОР ДНЯ
• Всего новостей: 45
• Топ сектор: CRYPTO (15)
• Средний impact: 78

💎 ТОП НОВОСТИ

🚀 Bitcoin достиг $100,000
Impact: 95 | Confidence: 90
Bitcoin установил новый исторический максимум...

[еще новости...]

📅 ЗАВТРА
• 14:00 - Решение ФРС по ставкам
• 16:30 - Отчет Tesla

Подготовлено @ReserveOne 📊
```

### 5. Экспорт данных

**CSV**: `/export/signals.csv`
- Все поля сигналов
- Фильтрация по параметрам
- UTF-8 кодировка

**PDF**: `/export/signals.pdf`
- Профессиональное оформление
- Графики и таблицы
- Брендирование

---

## 🗄 Структура базы данных

### Таблица: `signals`

```sql
CREATE TABLE signals (
    -- Идентификаторы
    id TEXT PRIMARY KEY,                -- UUID записи
    url_hash TEXT UNIQUE,               -- Хеш URL для дедупликации
    body_hash TEXT,                     -- Хеш контента
    
    -- Временные метки
    ts_published TEXT,                  -- Дата публикации
    ts_ingested TEXT,                   -- Дата сбора
    
    -- Источник
    source_domain TEXT,                 -- Домен источника
    url TEXT,                           -- URL новости
    
    -- Контент (английский)
    title TEXT,                         -- Оригинальный заголовок
    title_clean TEXT,                   -- Очищенный заголовок
    summary TEXT,                       -- Краткое описание
    
    -- Контент (русский)
    title_ru TEXT DEFAULT '',           -- Заголовок на русском
    summary_ru TEXT DEFAULT '',         -- Описание на русском
    deep_analysis_ru TEXT DEFAULT '',   -- Глубокий анализ (RU)
    deep_analysis_en TEXT DEFAULT '',   -- Глубокий анализ (EN)
    
    -- Классификация
    sector TEXT,                        -- Сектор экономики
    label TEXT,                         -- Метка
    region TEXT,                        -- Регион
    
    -- Сущности
    entities_json TEXT,                 -- JSON массив сущностей
    tickers_json TEXT,                  -- JSON массив тикеров
    
    -- Метрики
    impact INTEGER,                     -- Влияние (0-100)
    confidence INTEGER,                 -- Достоверность (0-100)
    sentiment INTEGER,                  -- Настроение (0-100)
    trust_score REAL DEFAULT 0.7,       -- Доверие к источнику
    
    -- Метаданные
    is_test BOOLEAN DEFAULT FALSE,      -- Тестовая запись
    merged_of TEXT,                     -- ID объединенных записей
    providers TEXT,                     -- LLM провайдеры
    latency TEXT DEFAULT 'fast',        -- Скорость обработки
    raw JSON                            -- Сырые данные
);
```

### Таблица: `ingested` (временная)

```sql
CREATE TABLE IF NOT EXISTS ingested (
    id TEXT PRIMARY KEY,
    ts_published TEXT,
    ts_ingested TEXT,
    source_domain TEXT,
    url_hash TEXT UNIQUE,
    url TEXT,
    title TEXT,
    summary TEXT,
    raw JSON
);
```

**Назначение**: Хранение необработанных новостей до анализа LLM.

---

## 🌐 API Endpoints

### Основные

#### `GET /`
Редирект на dashboard

#### `GET /dashboard`
Веб-интерфейс
- **Query params**: `lang` (ru/en)
- **Response**: HTML

#### `GET /signals`
Получить список сигналов
- **Query params**:
  - `sector` - фильтр по сектору
  - `region` - фильтр по региону
  - `min_impact` - минимальное влияние
  - `min_confidence` - минимальная достоверность
  - `min_sentiment` - минимальное настроение
  - `search` - поиск по тексту
  - `date_from` - дата начала (YYYY-MM-DD)
  - `limit` - лимит записей (default: 100)
- **Response**: JSON array

```json
[
  {
    "id": "abc-123",
    "title": "Bitcoin hits $100K",
    "title_ru": "Bitcoin достиг $100K",
    "summary": "Bitcoin reached new ATH...",
    "sector": "CRYPTO",
    "tickers": ["BTC"],
    "impact": 95,
    "confidence": 90,
    "sentiment": 85,
    "ts_published": "2025-11-06T12:00:00Z"
  }
]
```

#### `POST /ingest-run`
Запустить сбор новостей
- **Response**: JSON

```json
{
  "status": "success",
  "new_signals": 15,
  "message": "Pipeline completed"
}
```

#### `GET /health`
Проверка статуса системы
- **Response**: JSON

```json
{
  "status": "ok",
  "signals_count": 682,
  "last_update": "2025-11-06T14:30:00Z"
}
```

### Экспорт

#### `GET /export/signals.csv`
Экспорт в CSV
- **Query params**: такие же как в `/signals`
- **Response**: CSV file

#### `GET /export/signals.pdf`
Экспорт в PDF
- **Query params**: такие же как в `/signals`
- **Response**: PDF file

### Telegram

#### `GET /telegram-digest`
Генерация и отправка дайджеста
- **Query params**:
  - `send` (true/false) - отправить в канал
  - `top_n` (int) - количество новостей
- **Response**: JSON

```json
{
  "status": "success",
  "message": "Digest sent to @reserveone_ru",
  "news_count": 10
}
```

### Аналитика

#### `POST /generate-analysis/{signal_id}`
Регенерация анализа для сигнала
- **Path param**: `signal_id`
- **Response**: JSON

```json
{
  "status": "success",
  "signal_id": "abc-123",
  "analysis": {...}
}
```

---

## 🔄 Процесс анализа новостей

### Полный пайплайн

```python
async def run_pipeline():
    """
    Главный пайплайн обработки
    """
    async with pipeline_lock:  # Сериализуем выполнение
        logger.info("🚀 Pipeline started")
        
        # Шаг 1: Сбор новостей
        new_ids = await ingest_once()
        logger.info(f"📥 Ingested: {len(new_ids)} articles")
        
        # Шаг 2: Поиск orphan records
        orphan_ids = find_orphan_records()
        logger.info(f"🔍 Found orphans: {len(orphan_ids)}")
        
        # Шаг 3: Объединение для анализа
        to_analyze = list(set(new_ids + orphan_ids))
        
        # Шаг 4: Анализ через LLM
        if to_analyze:
            await analyze_batch(to_analyze)
            logger.info(f"✅ Analyzed: {len(to_analyze)} items")
        
        logger.info("🏁 Pipeline completed")
```

### 1. Ingest (Сбор)

```python
async def ingest_once():
    """
    Сбор новостей из RSS фидов
    """
    new_ids = []
    
    for sector in SECTORS:
        feed_url = RSS_FEEDS[sector]
        
        # Загрузка RSS
        feed = feedparser.parse(feed_url)
        
        for entry in feed.entries[:10]:  # Топ-10
            # Проверка дубликатов
            url_hash = hashlib.sha256(entry.link.encode()).hexdigest()
            
            if not exists_in_db(url_hash):
                # Сохранение в ingested
                article_id = save_to_ingested(entry)
                new_ids.append(article_id)
    
    return new_ids
```

### 2. Analyze (Анализ)

```python
async def analyze_batch(article_ids: List[str]):
    """
    Анализ новостей через LLM
    """
    tasks = []
    
    for article_id in article_ids:
        task = analyze_single(article_id)
        tasks.append(task)
    
    # Параллельный анализ (с семафором)
    results = await asyncio.gather(*tasks, return_exceptions=True)
    
    # Сохранение результатов
    for result in results:
        if not isinstance(result, Exception):
            save_signal(result)
```

### 3. LLM вызов

```python
async def call_llm(text: str, provider: str = "deepseek"):
    """
    Вызов LLM API с retry логикой
    """
    async with semaphore:  # Ограничение параллелизма
        prompt = generate_prompt(text)
        
        # Retry до 3 раз
        for attempt in range(3):
            try:
                if provider == "openai":
                    response = await call_openai(prompt)
                elif provider == "deepseek":
                    response = await call_deepseek(prompt)
                
                return parse_response(response)
            
            except Exception as e:
                logger.error(f"Attempt {attempt+1} failed: {e}")
                await asyncio.sleep(2 ** attempt)  # Exponential backoff
        
        raise Exception("All attempts failed")
```

---

## 🔌 Интеграции

### 1. OpenAI GPT-4

**Конфигурация**:
```env
OPENAI_API_KEY=sk-your-key-here
```

**Модель**: `gpt-4-turbo-preview`

**Стоимость**:
- Input: $10/1M tokens
- Output: $30/1M tokens

### 2. DeepSeek

**Конфигурация**:
```env
DEEPSEEK_API_KEY=sk-your-key-here
```

**Модель**: `deepseek-chat`

**Стоимость**:
- Input: $0.14/1M tokens (в 70 раз дешевле GPT-4!)
- Output: $0.28/1M tokens

### 3. xAI Grok (опционально)

**Конфигурация**:
```env
XAI_API_KEY=your-key-here
```

### 4. Telegram Bot

**Конфигурация**:
```env
TELEGRAM_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHANNEL_RU=@reserveone_ru
```

**API**: https://api.telegram.org/bot{TOKEN}/sendMessage

**Возможности**:
- Отправка дайджестов
- Markdown форматирование
- Эмодзи и стилизация

---

## 🚀 Установка и запуск

### Требования

- **Python**: 3.10+
- **Go**: 1.21+ (для Dashboard)
- **macOS**: 10.15+ (или Linux)
- **RAM**: минимум 2GB
- **Диск**: минимум 500MB

### Шаг 1: Клонирование

```bash
cd ~/
git clone <repository-url> signal-analysis
cd signal-analysis
```

### Шаг 2: Python зависимости

```bash
pip install -r requirements.txt
```

### Шаг 3: Конфигурация

```bash
cp env.example .env
nano .env
```

**Обязательные переменные**:
```env
# LLM (минимум один)
OPENAI_API_KEY=sk-your-openai-key
DEEPSEEK_API_KEY=sk-your-deepseek-key

# Telegram (опционально)
TELEGRAM_TOKEN=your-telegram-bot-token
TELEGRAM_CHANNEL_RU=@your_channel
```

### Шаг 4: Запуск

**Быстрый старт**:
```bash
python run.py
```

**Или ручной запуск**:
```bash
python app.py
```

**Проверка**:
```bash
open http://localhost:8080/dashboard
```

### Шаг 5: Go Dashboard (опционально)

```bash
cd go-signal-analysis
go build -o signal-analysis-server main.go
./signal-analysis-server
```

**Проверка**:
```bash
open http://localhost:8090/dashboard
```

---

## 🎛 Управление сервисами

### Скрипт управления

**Файл**: `manage_services.sh`

```bash
# Статус сервисов
./manage_services.sh status

# Запуск
./manage_services.sh start

# Остановка
./manage_services.sh stop

# Перезапуск
./manage_services.sh restart

# Логи
./manage_services.sh logs python
./manage_services.sh logs go

# Пересборка Go
./manage_services.sh rebuild
```

### Автозапуск (macOS)

**Файлы конфигурации**:
- `~/Library/LaunchAgents/com.signalanalysis.python.plist`
- `~/Library/LaunchAgents/com.signalanalysis.go.plist`

**Управление**:
```bash
# Включить автозапуск
launchctl load ~/Library/LaunchAgents/com.signalanalysis.python.plist
launchctl load ~/Library/LaunchAgents/com.signalanalysis.go.plist

# Отключить
launchctl unload ~/Library/LaunchAgents/com.signalanalysis.python.plist
launchctl unload ~/Library/LaunchAgents/com.signalanalysis.go.plist

# Проверить статус
launchctl list | grep signalanalysis
```

---

## ⚙️ Конфигурация

### RSS фиды (app.py, строка ~285)

```python
RSS_FEEDS = {
    "CRYPTO": "https://cryptonews.com/news/feed/",
    "TREASURY": "https://www.sec.gov/rss/news/news.xml",
    "BIOTECH": "https://www.fda.gov/rss/biotech.xml",
    # ...
}
```

### Активные секторы

```python
DEFAULT_SECTORS = [
    "TREASURY",
    "CRYPTO",
    "BIOTECH",
    "SEMIS",
    "ENERGY",
    "FINTECH",
    "COMMODITIES",
    "EMERGING_MARKETS",
    "TECHNOLOGY"
]
```

### Лимиты

```python
# Количество новостей из каждого фида
FEED_LIMIT = 10  # app.py:558

# Частота обновления
UPDATE_INTERVAL = 60  # минут, app.py:917

# Параллелизм LLM
SEMAPHORE_LIMIT = 2  # app.py:48
```

### Таймауты

```python
# HTTP запросы
HTTP_TIMEOUT = 30  # секунд

# База данных
DB_TIMEOUT = 60  # секунд
DB_BUSY_TIMEOUT = 30000  # миллисекунд
```

---

## 📁 Структура кода

### Основные файлы

```
signal-analysis/
├── app.py                      # Главный Python backend (2441 строка)
├── run.py                      # Скрипт запуска
├── debug.py                    # Диагностика
├── requirements.txt            # Python зависимости
├── .env                        # Конфигурация (секреты)
│
├── go-signal-analysis/         # Go Dashboard
│   ├── main.go                 # Главный файл
│   ├── handlers/               # HTTP обработчики
│   │   ├── dashboard.go
│   │   ├── signals.go
│   │   └── analysis.go
│   ├── models/                 # Модели данных
│   │   └── signal.go
│   ├── database/               # БД
│   │   └── db.go
│   ├── templates/              # HTML шаблоны
│   │   └── dashboard.html
│   └── static/                 # CSS/JS
│
├── signals.db                  # SQLite база данных
├── app.log                     # Логи Python
├── server.log                  # Логи Go
│
├── manage_services.sh          # Управление сервисами
├── open_dashboard.sh           # Быстрый запуск
│
└── docs/                       # Документация
    ├── README.md
    ├── START_HERE.md
    ├── SERVICES.md
    └── ПОЛНАЯ_ДОКУМЕНТАЦИЯ.md  # Этот файл
```

### Архитектура app.py

```python
# ===== ИМПОРТЫ (строки 1-28) =====
import fastapi, sqlite3, asyncio, httpx, feedparser

# ===== КОНФИГУРАЦИЯ (29-51) =====
DB_PATH = "signals.db"
TELEGRAM_TOKEN = os.getenv("TELEGRAM_TOKEN")
semaphore = asyncio.Semaphore(2)
pipeline_lock = asyncio.Lock()

# ===== БАЗА ДАННЫХ (52-150) =====
def db()                          # Подключение к БД
def find_orphan_records()         # Поиск незавершенных записей
def get_signals_from_db()         # Получение сигналов

# ===== RSS ФИДЫ (151-300) =====
RSS_FEEDS = {...}                 # Словарь фидов
DEFAULT_SECTORS = [...]           # Активные секторы

# ===== LLM ИНТЕГРАЦИЯ (301-550) =====
async def call_openai()           # OpenAI GPT-4
async def call_deepseek()         # DeepSeek
async def call_xai()              # xAI Grok
async def hybrid_call()           # Гибридный вызов

# ===== INGEST (551-700) =====
async def ingest_once()           # Сбор новостей из RSS
async def fetch_feed()            # Загрузка одного фида
def dedupe_by_hash()              # Дедупликация

# ===== ANALYZE (701-900) =====
async def analyze_batch()         # Пакетный анализ
async def analyze_single()        # Анализ одной новости
def parse_llm_response()          # Парсинг JSON от LLM

# ===== PIPELINE (901-950) =====
async def run_pipeline()          # Главный пайплайн
scheduler = AsyncIOScheduler()    # Планировщик

# ===== API ENDPOINTS (951-1200) =====
@app.get("/signals")              # Список сигналов
@app.post("/ingest-run")          # Запуск сбора
@app.get("/health")               # Проверка статуса
@app.get("/export/signals.csv")  # Экспорт CSV
@app.get("/export/signals.pdf")  # Экспорт PDF

# ===== TELEGRAM (1201-1400) =====
@app.get("/telegram-digest")      # Генерация дайджеста
async def send_telegram()         # Отправка в канал
def format_digest()               # Форматирование

# ===== WEB UI (1401-2441) =====
@app.get("/dashboard")            # HTML интерфейс
HTML_TEMPLATE = """..."""         # Встроенный HTML
```

---

## 🧮 Алгоритмы и утилиты

### 1. Дедупликация

**Метод**: SHA-256 хеширование URL

```python
def generate_url_hash(url: str) -> str:
    """
    Создает уникальный хеш для URL
    """
    return hashlib.sha256(url.encode('utf-8')).hexdigest()

def is_duplicate(url: str) -> bool:
    """
    Проверяет существование в БД
    """
    url_hash = generate_url_hash(url)
    
    conn = db()
    cursor = conn.execute(
        "SELECT 1 FROM signals WHERE url_hash = ? LIMIT 1",
        (url_hash,)
    )
    exists = cursor.fetchone() is not None
    conn.close()
    
    return exists
```

### 2. Нормализация тикеров

```python
def normalize_tickers(raw: str) -> str:
    """
    Преобразует: "BTCETHSOL" -> "BTC, ETH, SOL"
    """
    # Известные тикеры (2-5 символов)
    known_tickers = [
        "BTC", "ETH", "SOL", "AAPL", "TSLA", "NVDA", "MARA", "RIOT"
    ]
    
    found = []
    for ticker in known_tickers:
        if ticker in raw.upper():
            found.append(ticker)
    
    return ", ".join(found) if found else raw
```

### 3. Обрезка текста

```python
def truncate_by_words(text: str, max_words: int = 50) -> str:
    """
    Обрезает текст до N слов
    """
    words = text.split()
    
    if len(words) <= max_words:
        return text
    
    truncated = " ".join(words[:max_words])
    return truncated + "..."
```

### 4. Orphan records

**Проблема**: Новости попали в `ingested`, но анализ упал → остались без LLM.

**Решение**:
```python
def find_orphan_records() -> List[str]:
    """
    Находит записи в ingested, которых нет в signals
    """
    conn = db()
    cursor = conn.execute("""
        SELECT i.id
        FROM ingested i
        LEFT JOIN signals s ON i.id = s.id
        WHERE s.id IS NULL
        LIMIT 100
    """)
    
    orphan_ids = [row[0] for row in cursor.fetchall()]
    conn.close()
    
    return orphan_ids
```

### 5. Retry логика

```python
async def call_with_retry(func, max_attempts=3):
    """
    Повторные попытки с exponential backoff
    """
    for attempt in range(max_attempts):
        try:
            return await func()
        except Exception as e:
            if attempt == max_attempts - 1:
                raise
            
            wait_time = 2 ** attempt  # 1s, 2s, 4s
            logger.warning(f"Attempt {attempt+1} failed, retrying in {wait_time}s")
            await asyncio.sleep(wait_time)
```

---

## 🌍 Локализация

### Архитектура i18n

**Поддержка**: Русский (RU), Английский (EN)

**Механизм**:
1. HTML элементы с `data-en` и `data-ru` атрибутами
2. JavaScript словарь `i18n.translations`
3. Функция `i18n.t(key)` для перевода
4. Сохранение языка в `localStorage` и URL

### HTML элементы

```html
<button 
    data-en="Load Signals" 
    data-ru="Загрузить сигналы"
    onclick="loadSignals()"
>
    Загрузить сигналы
</button>
```

### JavaScript словарь

```javascript
const i18n = {
    currentLang: 'ru',
    
    translations: {
        ru: {
            'market_sentiment': 'Настроение рынка',
            'min_impact': 'Мин. влияние',
            'load_signals': 'Загрузить сигналы',
            // ...
        },
        en: {
            'market_sentiment': 'Market Sentiment',
            'min_impact': 'Min Impact',
            'load_signals': 'Load Signals',
            // ...
        }
    },
    
    t(key) {
        return this.translations[this.currentLang][key] || key;
    },
    
    setLanguage(lang) {
        this.currentLang = lang;
        this.updateUI();
        localStorage.setItem('preferred_language', lang);
    },
    
    updateUI() {
        // Обновляет все [data-en] и [data-ru] элементы
        document.querySelectorAll('[data-en][data-ru]').forEach(el => {
            el.textContent = el.getAttribute(`data-${this.currentLang}`);
        });
    }
};
```

### Переключение языка

```javascript
function toggleLanguage() {
    const newLang = i18n.currentLang === 'ru' ? 'en' : 'ru';
    i18n.setLanguage(newLang);
    
    // Обновить URL
    const url = new URL(window.location);
    url.searchParams.set('lang', newLang);
    window.history.pushState({}, '', url);
    
    // Перезагрузить данные
    loadSignals();
}
```

---

## 📊 Мониторинг и логирование

### Логи

**Python** (`app.log`):
```
2025-11-06 14:30:00 - INFO - 🚀 Pipeline started
2025-11-06 14:30:05 - INFO - 📥 Ingested: 15 articles
2025-11-06 14:30:10 - INFO - 🔍 Found orphans: 3
2025-11-06 14:30:45 - INFO - ✅ Analyzed: 18 items
2025-11-06 14:30:50 - INFO - 🏁 Pipeline completed
```

**Go** (`server.log`):
```
2025-11-06 14:35:00 [INFO] Server started on :8090
2025-11-06 14:35:10 [INFO] GET /dashboard - 200 OK (45ms)
2025-11-06 14:35:15 [INFO] GET /api/signals?sector=CRYPTO - 200 OK (12ms)
```

### Команды мониторинга

```bash
# Следить за логами в реальном времени
tail -f app.log
tail -f server.log

# Последние 100 строк
tail -100 app.log

# Поиск ошибок
grep ERROR app.log
grep -i "exception" app.log

# Статистика запросов
grep "GET /signals" server.log | wc -l

# Проверка портов
lsof -i :8080
lsof -i :8090

# Использование ресурсов
ps aux | grep python
ps aux | grep signal-analysis
```

### Health Check

```bash
# HTTP запрос
curl http://localhost:8080/health

# Ответ
{
  "status": "ok",
  "signals_count": 682,
  "last_update": "2025-11-06T14:30:00Z",
  "python_version": "3.10.12",
  "db_size_mb": 45.3
}
```

---

## 🗄️ Управление данными

### Очистка старых сигналов

**⚠️ ВАЖНО**: Система НЕ удаляет старые сигналы автоматически. Все данные хранятся вечно.

#### Проверить статистику

```bash
python3 cleanup_old_signals.py --stats
```

Покажет:
- Общее количество сигналов
- Распределение по датам
- Самую старую запись

#### Тестовый режим (безопасно)

```bash
# Показать что будет удалено (без реального удаления)
python3 cleanup_old_signals.py --days 30
python3 cleanup_old_signals.py --days 90
```

#### Реальное удаление

```bash
# Удалить все сигналы старше 30 дней
python3 cleanup_old_signals.py --days 30 --execute

# Удалить все сигналы старше 90 дней
python3 cleanup_old_signals.py --days 90 --execute

# Удалить всё старше года
python3 cleanup_old_signals.py --days 365 --execute
```

**Скрипт спросит подтверждение перед удалением!**

#### Ручное удаление через SQL

```bash
# Удалить сигналы старше 30 дней
sqlite3 signals.db "DELETE FROM signals WHERE DATE(ts_published) < DATE('now', '-30 days')"

# Оптимизировать базу после удаления
sqlite3 signals.db "VACUUM"
```

---

## 🔧 Устранение неполадок

### Проблема: "New signals: 0"

**Причины**:
1. API ключи не настроены
2. RSS фиды недоступны
3. Orphan records не обрабатываются

**Решение**:
```bash
# Диагностика
python debug.py

# Проверить .env
cat .env | grep API_KEY

# Проверить логи
tail -50 app.log | grep ERROR

# Форсировать анализ orphans
python force_analyze.py
```

### Проблема: "LLM API error"

**Причины**:
1. Неверный API ключ
2. Недостаточно средств на аккаунте
3. Rate limit превышен

**Решение**:
```bash
# Проверить ключ OpenAI
curl https://api.openai.com/v1/models \
  -H "Authorization: Bearer $OPENAI_API_KEY"

# Проверить ключ DeepSeek
curl https://api.deepseek.com/v1/models \
  -H "Authorization: Bearer $DEEPSEEK_API_KEY"

# Переключиться на DeepSeek (дешевле)
# В .env раскомментировать DEEPSEEK_API_KEY
```

### Проблема: "Database locked"

**Причины**:
1. Несколько процессов одновременно пишут в БД
2. Длинные транзакции

**Решение**:
```bash
# Остановить все процессы
./manage_services.sh stop
pkill -f "python app.py"

# Проверить WAL файлы
ls -lah signals.db*

# Удалить WAL (безопасно если сервер остановлен)
rm signals.db-wal signals.db-shm

# Запустить заново
./manage_services.sh start
```

### Проблема: Дубликаты в dashboard

**Причина**: Не работает дедупликация

**Решение**:
```bash
# Запустить скрипт очистки
python clean_duplicate_summaries.py

# Проверить результат
sqlite3 signals.db "SELECT COUNT(*), url_hash FROM signals GROUP BY url_hash HAVING COUNT(*) > 1"
```

### Проблема: Telegram не отправляет

**Причины**:
1. Неверный TELEGRAM_TOKEN
2. Бот не админ в канале

**Решение**:
```bash
# Проверить токен
curl "https://api.telegram.org/bot$TELEGRAM_TOKEN/getMe"

# Проверить канал
curl "https://api.telegram.org/bot$TELEGRAM_TOKEN/sendMessage" \
  -d "chat_id=$TELEGRAM_CHANNEL_RU" \
  -d "text=Test"

# Сделать бота админом:
# 1. Открыть канал в Telegram
# 2. Настройки -> Администраторы
# 3. Добавить бота
```

### Проблема: Медленная работа

**Решение**:
```bash
# Уменьшить количество новостей
# В app.py изменить:
for entry in feed.entries[:5]:  # было [:10]

# Увеличить интервал обновления
scheduler.add_job(run_pipeline, "interval", minutes=120)  # было 60

# Отключить ненужные секторы
DEFAULT_SECTORS = ["CRYPTO", "TECHNOLOGY"]  # только важные
```

---

## 📈 Статистика и метрики

### Текущая база

```sql
-- Общее количество
SELECT COUNT(*) FROM signals;
-- 682

-- По секторам
SELECT sector, COUNT(*) FROM signals 
GROUP BY sector ORDER BY COUNT(*) DESC;
-- TECHNOLOGY: 150
-- CRYPTO: 107
-- TREASURY: 95
-- ENERGY: 80
-- ...

-- Средние метрики
SELECT 
    AVG(impact) as avg_impact,
    AVG(confidence) as avg_confidence,
    AVG(sentiment) as avg_sentiment
FROM signals;
-- avg_impact: 72.5
-- avg_confidence: 78.3
-- avg_sentiment: 65.2

-- Топ источники
SELECT source_domain, COUNT(*) FROM signals
GROUP BY source_domain ORDER BY COUNT(*) DESC LIMIT 10;
```

### Производительность

- **Сбор новостей**: ~5 секунд (90 статей)
- **Анализ через DeepSeek**: ~1 секунда/новость
- **Анализ через GPT-4**: ~3 секунды/новость
- **Загрузка dashboard**: <100ms
- **SQL запросы**: <50ms

---

## 🎓 Дополнительные материалы

### Документация

- [README.md](README.md) - Краткое описание
- [START_HERE.md](START_HERE.md) - Быстрый старт
- [SERVICES.md](SERVICES.md) - Управление сервисами
- [config.md](config.md) - Конфигурация секторов

### Исправления

- [CRITICAL_FIXES.md](CRITICAL_FIXES.md) - Критические баги
- [DASHBOARD_USAGE.md](DASHBOARD_USAGE.md) - Использование UI
- [TELEGRAM_FIX.md](TELEGRAM_FIX.md) - Настройка Telegram

### Разработка

- Python Style Guide: PEP 8
- Go Style Guide: Effective Go
- Commit messages: Conventional Commits

---

## 📞 Контакты и поддержка

### GitHub

**Repository**: https://github.com/your-username/signal-analysis

### Issues

При обнаружении багов:
1. Запустите `python debug.py`
2. Соберите логи (`app.log`, `server.log`)
3. Создайте Issue с описанием

### Вклад в проект

```bash
# Fork репозиторий
git clone https://github.com/your-username/signal-analysis
cd signal-analysis

# Создать ветку
git checkout -b feature/my-feature

# Внести изменения
# ...

# Commit
git commit -m "feat: add new feature"

# Push
git push origin feature/my-feature

# Создать Pull Request
```

---

## 📜 Лицензия

MIT License

---

## ✨ Roadmap

### v2.0 (В разработке)

- [ ] Поддержка PostgreSQL
- [ ] WebSocket для real-time обновлений
- [ ] Экспорт в Excel
- [ ] Email дайджесты
- [ ] Мобильное приложение
- [ ] Кастомные RSS фиды от пользователя
- [ ] Alertы по ключевым словам
- [ ] Интеграция с Trading View

### v3.0 (Планируется)

- [ ] Machine Learning для прогнозов
- [ ] Sentiment анализ через FinBERT
- [ ] Графы зависимостей между секторами
- [ ] Backtesting торговых стратегий
- [ ] API для сторонних разработчиков

---

**📊 Signal Analysis - Professional News Analytics Platform**

*Создано с ❤️ для профессионального анализа рынков*

**Версия**: 1.5.0  
**Дата**: 6 ноября 2025  
**Автор**: SAA Alliance Team

