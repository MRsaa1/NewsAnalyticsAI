# 🚀 Управление Сервисами Signal Analysis

## Автозапуск

Оба сервиса настроены на **автоматический запуск** при загрузке системы через macOS launchd.

### Сервисы

1. **Python FastAPI Server** - http://localhost:8080
   - Обрабатывает RSS ленты
   - Анализирует новости через OpenAI/DeepSeek
   - Сохраняет сигналы в базу данных
   - Автоматически запускается каждый час

2. **Go Dashboard Server** - http://localhost:8090/dashboard
   - Отображает аналитическую панель
   - Показывает статистику и графики
   - Генерирует детальные отчеты

## 📋 Управление

Используйте скрипт `manage_services.sh` для управления сервисами:

```bash
# Показать статус
./manage_services.sh status

# Запустить сервисы
./manage_services.sh start

# Остановить сервисы
./manage_services.sh stop

# Перезапустить сервисы
./manage_services.sh restart

# Просмотр логов
./manage_services.sh logs python   # Логи Python сервера
./manage_services.sh logs go       # Логи Go сервера

# Пересобрать Go сервер
./manage_services.sh rebuild
```

## 🔧 Ручное Управление

### Остановить автозапуск

```bash
launchctl unload ~/Library/LaunchAgents/com.signalanalysis.python.plist
launchctl unload ~/Library/LaunchAgents/com.signalanalysis.go.plist
```

### Включить автозапуск

```bash
launchctl load ~/Library/LaunchAgents/com.signalanalysis.python.plist
launchctl load ~/Library/LaunchAgents/com.signalanalysis.go.plist
```

### Проверить запущенные процессы

```bash
lsof -i :8080 -i :8090
```

### Посмотреть статус в launchd

```bash
launchctl list | grep signalanalysis
```

## 📁 Файлы Конфигурации

- `~/Library/LaunchAgents/com.signalanalysis.python.plist` - Конфигурация Python сервиса
- `~/Library/LaunchAgents/com.signalanalysis.go.plist` - Конфигурация Go сервиса

## 📊 Логи

- **Python Server**: `~/signal-analysis/app.log`
- **Go Server**: `~/signal-analysis/server.log`

## 🔄 После Изменений в Коде

### Для Python:
```bash
./manage_services.sh restart
```

### Для Go:
```bash
./manage_services.sh rebuild
```

## ⚠️ Устранение Проблем

### Порт занят

```bash
# Найти процесс, использующий порт
lsof -i :8080  # для Python
lsof -i :8090  # для Go

# Остановить процесс
kill <PID>
```

### Сервис не запускается

1. Проверьте логи:
```bash
tail -50 ~/signal-analysis/app.log
tail -50 ~/signal-analysis/server.log
```

2. Проверьте .env файл:
```bash
cat ~/signal-analysis/.env
```

3. Перезапустите:
```bash
./manage_services.sh restart
```

### Полная перезагрузка

```bash
# Остановить все
./manage_services.sh stop

# Убить все процессы
pkill -f "uvicorn app:app"
pkill -f "signal-analysis-server"

# Запустить заново
./manage_services.sh start
```

## 🎯 Быстрые Ссылки

- **Dashboard**: http://localhost:8090/dashboard
- **API Signals**: http://localhost:8080/api/signals
- **API Stats**: http://localhost:8090/api/stats

## 💡 Полезные Команды

```bash
# Проверить статус и открыть Dashboard
./manage_services.sh status && open http://localhost:8090/dashboard

# Следить за логами Python в реальном времени
tail -f ~/signal-analysis/app.log

# Следить за логами Go в реальном времени
tail -f ~/signal-analysis/server.log

# Проверить использование портов
netstat -an | grep -E "8080|8090"
```

## 🔐 Безопасность

- Оба сервера слушают только на `localhost` (127.0.0.1)
- Доступ возможен только с локальной машины
- Для внешнего доступа используйте nginx или подобный reverse proxy

## 📝 Примечания

- Сервисы автоматически перезапускаются при сбоях (KeepAlive)
- Логи автоматически записываются в файлы
- После перезагрузки macOS сервисы запустятся автоматически
- Go сервер использует скомпилированный бинарник для быстрого запуска



