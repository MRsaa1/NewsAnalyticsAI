# ✅ Исправлена Автозагрузка Сигналов

## Проблема
Сигналы не загружались автоматически при открытии страницы http://localhost:8080/dashboard?lang=en

## Причина
В файле `app.py` на строке 1946 вызов функции `loadSignals()` был закомментирован:
```javascript
// НЕ загружаем автоматически! Пользователь сам выберет параметры и нажмет кнопку
// loadSignals();
```

## Решение
Добавлен автоматический вызов `loadSignals()` в обработчик события загрузки страницы:

```javascript
window.addEventListener('load', function() {
    // Инициализация языка
    i18n.setLanguage(initialLang);
    
    // Автоматически загружаем сигналы с параметрами по умолчанию
    loadSignals();
});
```

## Результат
✅ Сигналы теперь загружаются **автоматически** при открытии страницы  
✅ Применяются параметры по умолчанию (последние 36 часов)  
✅ Пользователь может изменить фильтры и перезагрузить сигналы кнопкой

## Тестирование

Откройте в браузере:
- http://localhost:8080/dashboard?lang=en (английская версия)
- http://localhost:8080/dashboard?lang=ru (русская версия)

Сигналы должны загрузиться автоматически при открытии страницы.

## Параметры по умолчанию

- **Период**: последние 36 часов
- **Лимит**: 50 сигналов
- **Минимальный Impact**: 0
- **Минимальная Confidence**: 0

Можно изменить фильтры и нажать "🔍 LOAD SIGNALS" для обновления.

---

**Дата исправления**: 2025-11-03  
**Файл**: app.py, строка 1963-1964  
**Статус**: ✅ Исправлено и протестировано



